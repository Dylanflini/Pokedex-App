import React from 'react'

function Chart( { baseState, name } ) {

  // function (baseState){

  // }

  return (
    <div>
      <p>{ baseState }</p>
      <p>{ name }</p>
    </div>
  )
}

export default Chart
